# SmartEnergy — dev / test / QA dependencies
-r requirements.txt

# Testing
pytest>=8.0
pytest-cov>=5.0

# Lint & format
ruff>=0.4

# Type checking (optional — strict mode not enforced yet)
# mypy>=1.10
