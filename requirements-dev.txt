# SmartEnergy — dev / test / QA dependencies
-r requirements.txt

# Testing
pytest>=8.0
pytest-cov>=5.0
httpx>=0.27

# Integration tests (Postgres + network-sim)
psycopg2-binary>=2.9

# Lint & format
ruff>=0.4

# Type checking (optional — strict mode not enforced yet)
# mypy>=1.10
